# grafana-credentials.yaml
# Grafana admin credentials
# NOTE: For local development only. Use proper secret management in production.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: echo-observability
  labels:
    app: grafana
type: Opaque
stringData:
  admin-password: "admin123"
