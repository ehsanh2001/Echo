# Service Configuration
NODE_ENV=development
PORT=8004
SERVICE_NAME=notification-service

# RabbitMQ Configuration
# Exchange name: RABBITMQ_EXCHANGE=echo.events (set in /echo/.env)
RABBITMQ_URL=amqp://admin:password@localhost:5672
RABBITMQ_QUEUE=notification_service_queue

# Email Provider Configuration
# For production: Use Resend
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# For testing: Use SMTP (MailHog)
# Set USE_SMTP=true to use SMTP instead of Resend
USE_SMTP=false
SMTP_HOST=localhost
SMTP_PORT=1025

# Common email settings
EMAIL_FROM_ADDRESS=onboarding@resend.dev
EMAIL_FROM_NAME=Echo Workspace

# Frontend URL (for email links)
FRONTEND_BASE_URL=http://localhost:3000

# User Service URL (to fetch inviter details)
USER_SERVICE_URL=http://localhost:8001

# Logging
LOG_LEVEL=debug
